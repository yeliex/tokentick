# 界面改造验证记录

本文件保留各阶段的验证证据。当前设计以需求文档及技术方案为准；下方历史记录中的测试数量、提交状态和界面描述仅适用于记录当时。

## 当前验证状态（2026-09-15）

- 数据库与解析精简已随 `6fb969f` 提交推送；周期详情时间展示已随 `c93661a` 提交推送。当前源码还包含 `2c0017f` 的文档空格修正；尚未发布安装包。
- 当前最近一次全量 Core 验证为 152 项、26 个测试组通过，日志 `.build/precommit-tests.log`。之后仅调整周期详情的时间文案与布局、文档。
- App 构建通过：`.build/cycle-detail-time-build.log`；CLI 构建通过：`.build/precommit-cli-build.log`。
- 最新 App 已退出并重新打开，确认启动显示服务端同步进度。最终详情布局有构建验证，尚未再次完成实机截图验收；此前的完整界面验收不能替代本次验证。
- 周期汇总查询在隔离数据副本中验证结果一致；性能数据及适用范围见文末 2026-09-15 记录。

## 历史验证记录

### 数据库精简前的界面验收

- 当前 checkout：master，保留已有未提交改动；本轮未提交、推送或发布。
- 最新完整 Core 测试：166 项、26 个测试组通过，日志 `.build/scoped-filters-tests.log`；最后归属优先级和选项边界调整后 13 项专项通过，日志 `.build/scoped-filters-targeted-tests.log`。
- 最新 App Debug 构建通过：`.build/recovery-error-build.log`；最新 CLI Debug 构建通过：`.build/final-current-cli-build.log`，当前构建产物 `tokentick --help` 退出码 0。
- 实机已验证：页面导航、每日与项目下钻、请求及统计详情弹窗、关闭保留条件、原生刷新期间周期控件稳定、自定义日期应用／重复编辑／取消、空结果恢复、系统半屏缩放，以及数据库打开失败后的重试恢复。
- 深色页面已有截图；最新浅色总览、用量明细、套餐列表和周期详情均取得完整窗口截图。三环与列表双向悬停、中心取消高亮、趋势柱形高亮和平均线已确认。
- 最终复核：资源、签名、性能、Escape 取消／关闭及状态保留已补齐验证。下文历史阶段的待办及锁屏阻塞已被后续结果覆盖；具体限制见最终验收范围。


## 已实现

- 三个主入口：总览、用量明细、套餐用量；独立设置包含通用、数据状态、存储和关于。
- 总览：当前确认登录账号的额度与预测状态；五个滚动周期、tokens／金额摘要、输入／输出／思考／缓存分项、Tokens 柱形／金额折线组合图、模型饼图及列表、最近对话下钻。
- 用量明细：每日／项目／任务聚合、账号及交叉筛选、搜索、排序、分页、摘要检查器、主内容区逐条消耗与证据；查询返回路径和窗口内明细状态保留。
- 套餐用量：历史七天窗口表格、日期／账号筛选、周期证据检查器。数据维护操作迁入设置。
- 登录环境代次隔离、进程内有界预测历史、自然日与滚动时间查询分离。技术方案记录计算与查询语义。

## 已验证的源码与受控测试

- `swift test`：150 项测试、26 个测试组通过。日志：`.build/ui-core-tests.log`。
- 本次新增预测、当前会话代次、滚动边界及总览一致性测试，覆盖账号切换、旧请求、观测不足／过期／零速度、恢复边界、样本上限、时间边界、未知账号、不同时区、最近对话排序与下钻金额／tokens 一致性。
- App Debug 最新 Luma 界面编译通过，半透明采用 Surfing 相同的纯 SwiftUI `containerBackground(.thinMaterial, for: .window)`，无新增 AppKit 窗口桥接。日志：`.build/luma-build.log`。
- 无变化刷新改造后全量 150 项测试通过（`.build/luma-tests.log`）；新增滚动查询内容比较测试后，总览专项 3 项通过（`.build/luma-result-tests.log`）。
- CLI Debug 编译通过。日志：`.build/ui-cli-build.log`。
- 测试夹具只用于受控测试，未写入用户用量库，也未当作 UI 真实数据。

## 已读取的真实运行态

2026-09-14 通过项目脚本启动 Debug App，并使用 Computer Use 读取 AX 树与窗口截图：

- 主导航显示三个入口，深色总览展示真实本地 tokens、API 等值金额、分项、模型和最近对话。
- 本地同步期间显示进度，API 返回后当前账号额度卡展示主桶和可取得的附加额度类型。
- 最新 Luma 版本：主额度默认独立展示，扩展额度入口可见；读取到扩展展开后的同名多周期分组。预测不足不再显示占位文案，进度差显示“结余”。无底部状态栏，右上工具栏显示同步时间。
- 最新窗口截图已确认纯 SwiftUI 半透明生效；明亮背景下深色次级文字对比不足已加背景色层修正，再次截图确认深色文字与卡片对比改善。
- 设置窗口可读到存储与关于内容。
- 最新版实际操作验证：总览进入用量明细、每日切换项目聚合成功；日期范围和全局 tokens／费用保持一致。截图确认主窗口无底部状态栏、侧栏与表格新布局。
- CLI `--help` 运行退出码为 0。

期间多次交互被 Computer Use 的“窗口已被用户切换，请重新读取状态”保护拒绝；最新同一次调用先读取再操作能完成导航与聚合切换，但选中行仍遇到元素 ID 失效和窗口定位失败。读取成功不等于操作验收通过；不能据此声称所有周期、下钻或窗口缩放已经验证。

## 尚待完成

- 当前 App 构建及 CLI 帮助运行已通过；最终全窗口视觉检查待完成。
- 五个周期与模型指标实际切换、图表悬停、最近对话下钻。
- 每日／项目／任务聚合、筛选／排序／分页、检查器、逐条明细与返回、跨页面状态保留。
- 套餐历史表格与检查器、数据状态入口和设置内容。
- 浅色与深色、窗口缩放、键盘、空／加载／错误状态的最终检查。
- 逐项完成需求与实现审计，检查最终 diff，不将测试成功代替真实 UI 验收。

## 2026-09-14 后续实机验收

- 正常启动恢复跟随系统深色；浅色验收仅使用当前进程的 DEBUG 环境变量，未改变系统偏好，验收后已退出该进程并正常重启。
- 实际切换 30 天、90 天和历史总和，分别看到对应汇总、模型、最近对话；历史显示未定价金额为未知，金额饼图排除未知模型金额。
- 最近对话下钻到任务，选中任务后打开检查器、进入逐条消耗、选中消耗查看精确费率／金额和来源证据成功。发现检查器旧文案和逐条表格过宽，已修正文案、缩减表格列与最小宽度；收尾效果仍需复查。
- 最新真实窗口确认：上次同步时间是刷新按钮前的独立文本；主额度无名称及信息图标，左结余右重置；扩展额度上下紧凑排列，使用接口显示名称，无预测。
- 最新截图确认单张组合图：Tokens 灰色柱形、金额青色折线，左轴 Tokens、右轴 USD，金额与 Tokens 单位清晰。无数据的金额不画成零；辅助功能使用原始金额，避免读出归一化后的绘图值。
- 独立设置浅色外观截图已读取；尚未完成所有维护操作和所有空／错误交互。

## 最新十二项调整

- 修正刷新按钮固定图标尺寸；额度加载为纯 spinner；移除扩展额度隐藏功能。
- 实机读到 ChatGPT Pro、可用重置 2 次及 credits 余额，未补写套餐倍率。主／扩展额度统一剩余；设置增加已使用切换，主额度增加日均、50% 和 80% 刻度。
- 总量／费用／五类 Tokens 合入同一卡片。趋势新增一年按周、历史按月、其余按日，增加平均虚线和悬停高亮；模型标题标签隐藏、配色调整、列表收窄，最近对话行距压缩。
- App 构建通过（`.build/luma-refinements-build.log`），8 项查询与额度元数据测试通过（`.build/luma-year-tests.log`），包括周一边界、跨时区、按日／月汇总和缺失元数据。
- 截图确认新主额度、套餐／次数／余额、固定刷新按钮、六周期及合并卡片。一年周期已实际切换；悬停高亮、设置显示方式切换和最后微调仍需实机复查。Computer Use 随后返回 `noWindowsAvailable`，未将其作为验收成功。

- 最新设置实机检查：通用中的“剩余／已使用”控件可见，并成功切换到“已使用”；随后窗口截图请求超时，主窗口联动效果还未取得最终截图。未通过 shell 改写用户偏好。

- 可用重置已移入主额度卡片底部，无图标；截图确认实际可用 2 次及最近到期时间。到期解析 5 项测试通过，构建通过（`.build/reset-expiry-tests.log`、`.build/reset-expiry-build.log`）。
- 按最新反馈，预估费用下方仅显示 USD，未完整计价信息移至悬停说明。


### 日志额度优先

已接入扫描结果到当前额度会话，按时间和窗口边界合并，并由完整主额度日志延后远端同步（日志后 5 分钟、API 开始后最多 30 分钟）。本次 CurrentLimitSession、AutomaticSync 和 LimitForecast 定向测试 21 项通过，追加的部分窗口合并测试所在 CurrentLimitSession 全部 8 项通过；macOS Debug 构建通过，diff 检查通过。日志：`.build/log-limits-tests.log`、`.build/log-limits-merge-tests.log`、`.build/log-limits-build.log`。本次未通过等待 30 分钟的真实网络抓取验证请求数量；频率验证来自可控时间的调度测试。


### 双主额度卡片与重置明细

5 小时移除刻度，周额度保留刻度；24 小时内的重置改为今天／明天的具体时间。全部已返回的有效重置到期时间在同一行显示（支持横向滚动），credits 移入主额度卡片。CurrentLimitSession 8 项测试通过，Debug 构建通过。使用独立临时数据库和临时编译的 SwiftUI 预览展示 5 小时 28%、每周 62%、3 个不同重置到期时间和 credits；已通过 Computer Use 核验截图 `.build/quota-five-hour-preview.png`。预览数据仅在预览产物中，临时源码已恢复，不写入真实用量数据库。当时使用标记为“布局预览”的独立预览构建，验证后已恢复正常数据启动。

双周期紧凑布局已通过独立 SwiftUI 预览构建及 Computer Use 核验，5 小时和每周百分比右对齐、窗口间距 18pt；重置明细和 credits 使用一致的行内对齐。截图 `.build/quota-compact-preview.png` 使用示例数据，临时预览注入源码已恢复。侧边栏整行点击已在真实 App 的文字右侧留白实测切换成功。

数据库打开状态修复：移除 ApplicationModel.store 的 ObservationIgnored，使连接就绪直接通知界面。使用独立空数据库且 TOKENTICK_AUTOSYNC=0 冷启动，无点击或切换页面即显示总览，Computer Use 已核验。空用量状态占满内容宽度并居中，仅保留图标和标题，移除说明；Debug 构建通过（.build/store-observation-build.log）。

金额统一使用 $，删除重复 USD 标注；最新 Debug 构建通过（.build/currency-symbol-build.log）。

## 用量明细与套餐用量重构（2026-09-14）

当前阶段按需求第 10 节实施；代码未提交。总览保持已有实现。

- 用量明细改为消耗去向工作区：统一汇总、可见的项目／模型／账号选项、每日／项目／任务切换、直接下钻与返回。原始记录未选中时占满内容区。
- 套餐用量改为周期列表与详情并排，展示最后记录的使用量、记录时间、本地 Tokens／费用和已记录的重置。统计依据默认折叠；周期以“未到期／已到期”描述，避免把重置前的重叠周期都称为当前周期。
- Debug 构建通过：`.build/details-redesign-final-build.log`。OverviewReportTests、RollingUsageTests、LimitQueryTests 共 20 项定向测试通过：`.build/details-redesign-tests.log`。新增筛选选项去重与未知值处理测试。
- Computer Use 在真实数据上核验了每日、项目列表及周期列表／详情布局。每日 2026-09-12 下钻至任务、打开 3 条消耗并返回，汇总保持 563,558 Tokens、$2.12，日期筛选保留。核验了两个不同周周期的详情切换。
- 最终构建已正常启动，读取到最近 30 天的项目聚合与实际项目列表。窗口内页面状态保留由主窗口持有；最后一轮修正了返回后日期预设、未知额度不显示为零以及重置早于推算起点的文案。
- 项目到任务的完整路径、全部筛选组合／排序／分页、最小窗口宽度以及错误恢复尚未完成实机验收。验证期间多次遭遇窗口被切换的保护拒绝与状态变化，未把这些操作计为通过。

### 周期状态与请求次数修正

周期缓存版本更新，单账号周期跨未知账号观测也按时间衔接；实际重置只关联紧接的下一周期，避免使用更晚周期的重置。请求次数来自本地用量查询 COUNT(*)，移除统计依据面板，已确认提前重置改为手动重置。15 项 LimitQueryTests 通过（`.build/cycle-status-tests.log`），覆盖未知账号交错、历史分页状态及重置边界请求计数。Debug 构建通过（`.build/cycle-status-build.log`）。

### 用量明细稳定性重构

读取 2026-09-14 17:49、17:52、18:03 三份崩溃报告，均为 SIGABRT，异常堆栈涉及 NSWindow 更新约束、SplitViewChildController 与 NSHostingView 尺寸更新。移除用量明细 inspector 与逐条请求页动态 HSplitView，改为明确的详情按钮和固定 600×580 的可滚动 sheet；详情采用打开时的数据快照。后台查询持有任务句柄并传递取消，逐条结果不变时不重写列表。

Debug 构建通过（`.build/details-stability-build.log`）；总览与滚动查询 7 项测试通过（`.build/details-stability-tests.log`）。Computer Use 已验证 2026-09-12 统计详情 → 三条请求 → 请求详情 → 关闭，以及总览／用量明细连续三轮切换，过程中未观察到闪退。未据此声称长期运行不再崩溃。

用量明细模块重排已通过 Debug 构建（`.build/details-layout-build.log`）及 Computer Use 截图核验：聚合与筛选在前，单行汇总紧接表格，1120×762 窗口可见行数由 7 行增加到 9 行。当前崩溃目录最新报告仍是 18:03 的旧版本报告，短时验证未产生新报告。

原生工具栏与筛选布局：Debug 构建通过（`.build/details-filters-build.log`）；总页数、超出末页、未知分组与搜索筛选相关的总览／滚动测试 8 项通过（`.build/details-pages-tests.log`）。实机截图核验无标题／时区、排序前置、表格无嵌套背景，页脚显示“第 1 / 1 页 · 共 1 条”。总览点击原生刷新按钮，刷新前与刷新中截图的统计周期位置及尺寸一致。

按天筛选及请求明细弹窗：Debug 构建通过（`.build/details-modal-build.log`）。实机点击 2026-09-12，日期选择器更新为该日，任务列表为 563,558 Tokens、$2.12、3 次请求；点击任务以 sheet 打开 3 条请求，关闭后日期与汇总保留，截图无返回层级／额外筛选条。

统一时间档位及自定义日期弹窗：Debug 构建通过（`.build/date-presets-build.log`），diff 检查通过。已正常启动最新构建；Computer Use 验证时设备锁屏，未完成本轮预设切换与弹窗应用／取消的实机验收。

## 总览三环统计（2026-09-14）

- App 构建通过：`.build/nested-rings-build.log`。统计、轮次归因 16 项测试通过：`.build/nested-rings-tests.log`；日志扫描、设置归因 28 项测试通过：`.build/nested-rings-parser-tests.log`。覆盖互斥模式、缺失深度、周期边界、金额一致性、历史回填及旧版本游标重扫去重。
- 实机已显示三层同心环、右侧三组列表；切换金额后模型和使用模式占比同步变化。首次启动自动重扫 1,581 个历史文件，回填完成前缺失的推理深度按未知显示。
- 实机“所有”周期已显示快速＋长上下文、长上下文，以及极高／高／中／超高／低等真实推理深度分组；金额缺失显示“—”。已完成部分历史回填，剩余日志继续自动扫描。

## 历史回填与日期取消验收（2026-09-14）

- 只读数据库确认全部 1,581 个 `scan_files` 游标升级到版本 7。真实请求已回填 high、xhigh、medium、low、max、ultra；缺失字段仍按未知处理。
- 用量明细日期菜单实机显示 1天、7天、30天、90天、1年、所有、自定义。自定义弹窗中开始日期从 2026/8/16 改为 2026/9/12 后取消，主页面仍为 30天、2026-08-16 — 2026-09-14。应用操作未完成，不能据此声称日期应用通过。
- 后续 Computer Use 操作连续遇到用户前台状态变化保护，未继续抢占操作。窗口缩放等未完成项保留在最新审计中。
- 最新全量 Core 165 项测试通过，App / CLI 构建通过，最新 CLI `--help` 退出码 0；未提交、推送或发布。

## 三环留白与并排列表（2026-09-14）

- 环半径为 101／73／45，默认描边 18（按最新反馈加粗一倍），中心无文字；悬停命中扇区使用描边 24 和 1.025 倍缩放，并联动列表底色。图与列表双向高亮，减少动态效果时关闭过渡。
- 三组列表横排，宽度不足时图上移，列表仍并排；删除位置说明，未知在各组末尾。App 构建通过：`.build/rings-hover-build.log`；实机已打开新版并读取到模型／使用模式／推理深度三列。鼠标悬停放大和窄窗口切换仍需最终实机确认，不以源码检查代替。

## 紧凑筛选与项目分类（2026-09-14）

- 筛选行使用 small 控件、6 点间距，收紧日期／搜索／项目／模型／排序宽度。移除未知模型和未知项目选项，按当前日期及账号范围取有效用量中的选项。
- 从 Codex 已保存项目、任务归属及聊天标记刷新名称，取消任意工作目录名回退；聊天显示为“聊天”。实机 30 天项目菜单确认包含聊天、Surfing、TokenTick、m2l-console、youzan-web 等，聊天目录名不再出现。
- `.build/scoped-filters-tests.log` 全量 166 项通过；最后归属优先级和选项边界调整后 `.build/scoped-filters-targeted-tests.log` 13 项通过；App 构建 `.build/scoped-filters-build.log` 通过。

## 日期 popover 与半屏窗口实机验收（2026-09-14）

- 选择自定义后，AX 树明确为锚定日期菜单的弹出窗口。将开始日期改为 2026/9/13、结束日期保持 2026/9/14，点击应用后列表只保留对应两日；截图确认汇总为 64.52M Tokens、$37.55、485 次请求，日期范围正确。
- 再次选择自定义，popover 保留 9/13 — 9/14；取消后保持查询。搜索不存在的标题显示“所选范围暂无用量”及 0 条，清空搜索恢复两条。测试结束已恢复原 30 天筛选。
- 通过系统窗口菜单切换左侧半屏，截图确认紧凑筛选全行、汇总卡片及表格均可见；总览三环上移，三组列表仍横排，空心与环间留白可见。随后使用系统“恢复上一个大小”。
- SwiftUI 合并 AX 文本会保留旧汇总值，本轮以实际截图和日期控件值交叉验证，不据此误判数据未刷新。
- 鼠标落点截图未提供足够证据证明扇区放大／高亮联动，继续保留该项；最新浅色、错误恢复亦未声称通过。

## 浅色与数据库错误恢复验收（2026-09-14）

- 通过现有 DEBUG 的 TOKENTICK_APPEARANCE=light 仅覆盖测试进程，不修改系统偏好。浅色总览截图确认三环、三列列表、趋势与最近对话可读；实际截图捕获“低”推理深度行背景高亮、对应蓝色内环强调及其他扇区弱化，列表到环图的悬停联动已生效。浅色明细截图源多次只返回窗口缩略图，未据此声称全部页面浅色细节已验证。
- 使用 .build 下独立路径（以目录占用数据库文件路径）制造打开失败，实机显示“无法打开数据库”及重试按钮。随后把真实数据库通过 SQLite backup 复制到该路径，点击重试成功恢复 28 行每日记录和真实汇总。
- 本次发现并修正 ApplicationModel.start 重试时未清理旧错误：在新启动尝试开始清空 error，新的失败仍正常写入。修正后再次完整执行失败／修复路径／重试，实机设置数据状态确认旧错误消失。构建通过：`.build/recovery-error-build.log`。
- 测试后关闭隔离进程，恢复默认数据库和跟随系统的外观；独立数据库副本已清理。没有改写真实数据库或发布代码。

## 浅色页面与直接扇区悬停验收（2026-09-14）

- 当前真实数据库下，浅色用量明细完整截图确认紧凑筛选、右侧日期范围、汇总与表格可读；套餐用量从居中 loading 正常转为历史周期列表和单张详情卡，最新周期为进行中，历史周期为已结束。
- 总览趋势实机显示 9 月 10 日提示、加深的 Tokens 柱形，以及 Tokens／金额平均虚线。
- 通过 Computer Use 将指针分别定位到模型、使用模式、推理深度扇区，截图确认 gpt-6-astra／快速／高对应行高亮、扇区加粗放大、其他扇区弱化；移到空心中心后全部恢复。使用的点击只用于定位指针，图表没有点击选择逻辑。
- 验收后关闭仅覆盖浅色外观的测试进程，正常启动默认数据库和系统外观；未修改系统偏好。最新 CLI 构建与 --help 通过，diff 检查通过。

- 当前 Debug App 与 CLI 均为 arm64；App 的 `codesign --verify --deep --strict` 通过，签名为 ad-hoc，封装资源校验通过。此处验证当前 Debug 产物，不代表已生成或发布新的 ZIP。

## 当前产物性能与资源核验（2026-09-14）

- 使用当前 Debug CLI，在独立目录 `.build/audit/ui-final-b5dao6jx` 复用既有性能脚本：4 个日志文件共 2,000 条双格式请求，重复扫描后各追加 25 条，共 2,100 条。只写隔离数据库，未作为真实 UI 数据；原始输出和脚本保留在该目录。
- 以下为单次进程端到端观测（含启动），不承诺稳定延迟，不与不同机器或构建配置直接比较。

| 操作 | 耗时 | 最大驻留内存 |
| --- | ---: | ---: |
| 首次扫描 | 4.553 s | 34.12 MiB |
| 无变化扫描 | 0.019 s | 11.72 MiB |
| 增量扫描 | 0.260 s | 17.34 MiB |
| UTC 总量查询 | 0.029 s | 18.03 MiB |
| 2,100 条请求查询 | 0.308 s | 63.73 MiB |

- App、CLI bundle 的内置价格 JSON 与源码 SHA-256 完全一致；复制 CLI 和资源 bundle 到独立目录后，`prices` 成功读取并返回非空 JSON。记录：`.build/audit/ui-final-b5dao6jx/resources.json`。App 资源中包含 Assets.car、TokenTick.icns、GRDB 和 Core bundle。
- 本轮键盘验收受阻：Computer Use 明确返回 Mac 已锁定且无法自动解锁；未绕过锁屏，不将此前鼠标操作算作 Escape／键盘验收通过。需手动解锁后继续。

## 最终验收范围（2026-09-14）

设备解锁后继续验收：日期 Menu → 自定义 popover → Escape，弹层关闭且仍保持原 30 天范围；总览最近对话 → 请求明细 sheet → Escape，弹窗关闭后保留用量明细原有任务聚合、筛选、选中行及第 1 / 4 页。请求表格的方向键未观察到选择变化，未宣称其支持行选择；表格没有配置 selection，详情通过按钮打开。辅助功能点击任务行只选中行，也不作为打开名称按钮的证据。

| 需求 | 实现与验证依据 | 结论 |
| --- | --- | --- |
| 主导航、设置与数据状态 | ContentView、Settings scene、DataStatusView；真实页面和设置切换、同步状态、错误恢复 | 已验证 |
| 当前账号、所有可得窗口、主／扩展层级 | CurrentLimitSession、CurrentLimitsView；真实当前账号、套餐、周额度、Spark、重置和 credits；双主窗口布局另有明确标记的预览 | 已实现；真实账号未同时返回主桶五小时，未把预览当成真实额度 |
| 预测、账号隔离、过期与恢复边界 | 技术方案 6.4、LimitForecastTests 六项、CurrentLimitSessionTests 八项、自动同步测试；无实际样本时不伪造预测 | 受控测试通过；未为验收切换用户登录账号或消耗额度 |
| 六周期、分项、组合趋势、三环与最近对话 | OverviewReportTests 覆盖精确范围、分桶、互斥分组、无变化结果；实机周期和金额／Tokens 切换、三环双向高亮、趋势提示及平均线 | 已验证 |
| 明细聚合、筛选、分页、弹窗与证据 | UsageDetailsView、UsageTableView、UsageRecordsView；前述真实下钻、筛选、日期应用／取消、空结果恢复和弹窗滚动记录；最终 Escape 检查 | 已验证；多账号边界采用测试，当前设备单账号 |
| 历史七天、真实提前结束、单周期状态与单卡详情 | LimitQueryTests、WeeklyCycle 查询；真实多个周期切换及深浅色截图 | 已验证 |
| 半透明 Luma、无底栏、原生刷新、宽度与稳定状态 | SwiftUI window material、原生 ToolbarItem、无变化内容比较；完整深浅色截图、半屏窗口、刷新前后及页面反复切换 | 已验证；短时运行不承诺长期零崩溃 |
| 空／加载／错误与恢复 | 真实空搜索、额度及历史 loading、隔离数据库打开失败／重试完整流程 | 已验证；故障注入未改写真实用量库 |
| Core 不重复计量与计价证据 | 最新 166 项全量测试及最终 13 项专项；原始请求和价格逻辑沿用 Core | 通过 |
| App／CLI、架构、签名与资源 | 最新构建日志、--help、arm64、codesign 严格验证、bundle 哈希和独立 CLI 读取 | 通过；未发布新 ZIP |
| 性能记录 | 当前 Debug 隔离样本首次／无变化／增量、查询及最大驻留内存实测 | 已记录，非生产性能承诺 |

源码、构建和真实 UI 证据按上述范围使用。完整 VoiceOver 遍历、其他 macOS 版本及长期压力测试不在本次完成门槛内；没有将工具返回的窗口缩略图当成完整视觉验证。保留工作区改动，未经另行授权不提交、推送、发布或合并。

## 过期额度静默刷新（2026-09-15）

- 移除超过 15 分钟后替换整张卡片的“额度已过期，请刷新”；同一登录会话内继续渲染已有快照。主窗口激活时检查时效并触发一次 API 同步，若已有同步则等待结束后复查；取消激活任务会停止等待。
- 登录变化清空快照和拒绝旧请求的逻辑保留，过期观测仍不能生成预测。没有新增磁盘额度快照或跨进程恢复逻辑。
- App Debug 构建通过（`.build/stale-limits-build.log`），CurrentLimitSessionTests／LimitForecastTests 共 14 项通过（`.build/stale-limits-tests.log`），diff 检查通过。现有测试覆盖账号隔离和预测过期边界；本轮未等待真实快照老化 15 分钟进行窗口激活实测，不将构建或 Core 测试作为该运行过程的实测证据。

### 2026-09-15 总览查询字段优化

- 新增 reasoning_effort 持久字段，v13 从已有证据回填；新请求及重扫升级同步写入。快速模式复用 tier 与已有 fast_trace 主键记录，时间范围增加 occurred_at 索引。
- 21 项 OverviewReport／TurnUsage／FastEvidence 测试通过，覆盖迁移、缺失推理深度、重扫去重及显式普通档位优先于 trace；Debug App 构建通过。
- 同一真实数据库副本、相同 now／Asia/Shanghai 时区，对照 HEAD 的旧查询：六个周期的完整报告内容均一致。单轮 Debug 耗时：1 天 1.46→0.11 秒，7 天 2.21→0.60 秒，30 天 4.90→1.99 秒，90 天 6.74→4.49 秒，一年 9.83→5.47 秒，历史总和 8.95→5.21 秒。这是查询耗时，不是 GUI 首屏计时，也不包含一次性迁移。
- 本地复现代码、测量与结果保存在 .build/audit/overview-perf-y88_z0mv/{baseline.swift,main.swift,fields-comparison.json}；正式数据库未用于写入实验。通用聚合重复查询仍保留，后续优化可单独处理。

### 2026-09-15 开发阶段数据库收敛

- 当前建表定义替代历史迁移链，旧开发结构直接重建。删除 turn_usage、statistics_rebuild、weekly_limit_observations；usage 与 weekly_limit_cycles 不再包含 JSON 列。
- 请求明细保存去重所需累计分项、轮次起点及计价选择；来源详情改为结构化字段。日常汇总按变更日期刷新，全量重建事务内完成。已结束的周期仅区分自然／提前重置，当前窗口不落库，重启恢复日志后继续增量。
- 全量 149 项 Core 测试通过；后续恢复路径、轮次匹配、周期及筛选专项回归通过。App 和 CLI Debug 构建成功，git diff --check 通过。
- 真实来源重建使用独立测试库：1,589 个日志文件、9,618,882,702 字节，126,010 条请求，采集问题 0。按 rollout_id＋source_line 对照当前旧库，全部 126,010 条请求的总量及 Tokens 分项一致；旧库额外 4 条均在本次扫描游标之后，没有本次已扫描请求缺失。
- 测试库去除验证用临时索引并压缩后为 93,294,592 字节，旧库当时逻辑文件大小为 1,019,912,192 字节。真实日志、原库均未写入修改。
- 最终代码在测试库上复测：1 天 0.079 秒、7 天 0.455 秒、30 天 1.351 秒、90 天 2.388 秒、一年 3.712 秒、历史总和 1.662 秒。均为单轮 Debug Core 查询，不等于 GUI 首屏时间。
- 复现资料位于 .build/audit/schema-rebuild/（main.swift、scan.json、comparison.json、verified-measurements.json）。首次验证脚本在事务内调用 checkpoint 导致结束报错，发生在数据和测量保存之后；已修正为事务外调用并成功复验。首次扫描中曾为旧验证程序临时补充归属索引，最终代码复用 turn_key 索引，因此首次 629.7 秒仅记录实验过程，不作为最终版本扫描性能。
- 本轮未替换正在运行的 App；新构建首次打开旧开发库会重建并重新采集。


### 2026-09-15 套餐用量读取优化

- 历史周期保存 total_tokens、request_count、amount、known_amount，页面查询不再访问 usage。同步补入请求或修正边界后更新，重新计价后同步更新；事实版本与周期均不变时跳过计算。
- 删除周期表中可由 account_id 推导的 scope_key，缺失账号不推断归属。单一建表定义调整为 schema.2，未上线数据库仍按既定规则重建，不新增迁移链。
- 28 项相关测试通过，随后新增的周期汇总测试随 5 项 LimitQueryTests 通过；覆盖跨边界轮次、提前重置、账号隔离、不完整金额、补入日志、持久化读取。App 构建成功；未重启正在运行的 App。
- 对已有真实数据副本（126010 条请求、61 个历史周期）查询同一页 20 个周期：原 SQL 21.74 秒，保存汇总后读取约 0.11 毫秒；一次更新全部周期约 0.85 秒，两种查询结果一致。这是 SQLite 查询测量，不是完整 UI 渲染耗时。
- 脚本及数据副本：`.build/audit/cycle-query/benchmark.py`、`results.json`、`usage.sqlite`。测试日志：`.build/cycle-summary-tests.log`、`.build/cycle-summary-extra-tests.log`；构建日志：`.build/cycle-summary-build.log`。

### 2026-09-15 提交前验证

- 最终结构为 schema.3，价格使用结构化规则和来源字段；扫描断点继续保留 JSON，仅精简无用状态，解析版本为 8。
- 全部 152 项 Core 测试通过（`.build/precommit-tests.log`），包含重启后保留当前轮次设置、fork、去重、文件替换与截断。App 构建通过（`.build/parser-state-build.log`）。
- 最新 App 已通过系统 UI 退出并重新打开，启动时显示“读取服务端用量…”及 loading；日志重建完成及完整界面表现尚未再次验收。

### 2026-09-15 菜单重构、当天筛选与刻度

本节记录本次菜单任务的验证，前文为各阶段历史记录，不表示已复验最终菜单样式。

- 代码已实现：320 点半透明自适应高度菜单、两行订阅头部及统一 loading、紧凑主额度／重置次数／Credits、四个统计周期、主页面及退出入口。移除菜单设置、刷新、检查更新入口；底部行高 24 点，整行 hover 高亮。
- 图表最终实现：125 点绘图区、双指标峰值、Tokens 柱形与金额折线、两条日均虚线、起止日期、K／M／B 缩写；无图例、纵轴数字、常规参考线和底部说明。hover 高亮、定位线与详情浮层保留。
- 设置增加 4／5／7 天均分刻度；菜单与总览使用绿色竖线标记自然时间进度。总览和明细筛选统一为“当天”，总览改用统计时区零点至当前时间；菜单今天复用同一查询规则。
- 本次执行的 `swift test --filter LimitPacingTests`：2 项通过，覆盖均分刻度、窗口起止边界、5 小时窗口及非有限时间。`swift test --filter OverviewReportTests`：10 项通过，覆盖当天范围、跨时区、夏令时及摘要／趋势／模型／任务汇总一致性；不是对最终工作区的全量回归声明。
- 本次各轮 App 构建与启动检查通过；最后一次图表调整使用 `./script/build_and_run.sh --verify`。构建日志路径为 `.build/logs/app-build.log`，该路径会被后续构建覆盖，不是不可变证据归档。
- 实际界面确认：Computer Use 曾读取到总览的真实额度、绿色时间标记、重置次数及 Credits，设置中的 4／5／7 天选项可见。用户提供的菜单截图用于确认行对齐、密度及双纵轴挤占绘图区问题。
- 验证边界：Computer Use 未完成最终弹出菜单的完整视觉与交互复验。无纵轴的新图表、平均线、hover 浮层边缘表现、最终入口对齐，以及不同屏幕尺寸和深浅色下的完整布局仍需实机确认；构建成功不等同于这些项目已验收。


### 2026-09-15 窗口、数字图标与提交范围验证

以下结果对应代码提交 `eb10710`，补充前面的阶段记录。

- 已实测：图标旧实现出现无响应时，主线程采样反复进入菜单栏图标更新及布局路径；移除标签内 TimelineView、改用缓存图片后，Computer Use 能再次读取并操作主窗口。此前仅构建与启动成功不能证明图标交互正常。
- 已实测：⌘, 能显示独立设置窗口，⌘W 能关闭设置窗口。最终单主窗口构建中发送 ⌘W 后，通过只读查询确认进程仍运行且 activationPolicy=1（accessory），即关闭窗口后转入隐藏 Dock 的模式。Computer Use 后续读取可能重新激活或打开窗口，因此不单凭读取到窗口就判定关闭失败。
- 源码已实现、尚未完整实测：仅剩菜单栏入口时，在弹层中按 ⌘, 先恢复主窗口再打开设置；主窗口重复打开复用、最小化窗口保留 Dock，以及菜单弹层打开时 Dock 不出现的完整组合。⌘R、⌘Q 保留现有绑定，本轮未逐项重新执行；不宣称所有快捷键已通过验收。
- 数字图标已实现整数显示、原尺寸、收紧字距和模板着色。最后字距修改已构建，但 0／两位数／100、深浅色、菜单选中状态的清晰度及数字更新未完成完整视觉复验；当前无专用悬停百分比提示。
- 提交前运行 `swift test --filter 'LimitQueryTests|LocalUsageScannerTests|CodexAPITests|LimitPacingTests|OverviewReportTests'`，47 项测试通过。覆盖跨重启和跨文件周期结束、批次游标恢复、连续周期、当天及夏令时边界、额度刻度；不是全量测试声明。
- 从暂存区导出本次提交范围单独构建，补齐 MenuUsageView 的 Xcode 编译引用后 `BUILD SUCCEEDED`。该构建不包含工作区中另一个任务的自动更新／发布和模型列表高度改动。构建日志位于 `/tmp/tokentick-commit-build.log`，线程采样位于 `/tmp/tokentick-hang-sample.txt`；均为本机临时证据，不作为长期归档承诺。
