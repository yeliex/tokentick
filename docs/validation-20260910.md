# 2026-09-10 真实历史数据验证

先使用默认数据库的 SQLite 一致副本独立验证；副本验证完成后，再启动新版 App 升级默认数据库。基线为 `4234b0b2ec869734f626f75f52edb4ef0f66fc0e`；后续本轮修复只改变周额度历史查询与不兼容数据库的错误说明。

环境：Apple M2 Pro、32 GiB，macOS 27.0（26A5425a），Xcode 27.0（27A266a）。构建最低系统为 macOS 26.0，仅 arm64；用户随后确认按当前设备验收，无需额外 macOS 26 实机验收；最低部署版本保持 26.0。

## 数据与一致性

- 基线 168,874 条请求，日志覆盖 2026-02-04 至 2026-09-10，共 1,540 个 rollout 文件。
- v4 → v5 迁移保留所有用量事实，迁移原有 123 条周额度观测，五小时历史不进入新业务表；原始快照及自动迁移备份保留。
- 解析器升级后的完整重扫没有解析问题。原有行的 ID、去重键、发生时间、计价日期、所有 token 分项和来源逐条散列一致；活跃日志新增请求单独计入。
- 首份价格回填后完整定价记录由 3,768 增至 91,032，计价无非法用量及溢出；仍未知的记录分别缺模型、价格或 Fast 模式，未强行计价。价格快照日期保持原采集日期。
- 独占应用写锁并使缓存版本失效后，分别查询总量、日、任务、项目和模型五个维度，确认期间没有重建缓存。释放锁重建后，五份输出逐字段相同；总 tokens 与原始事实 SQL 求和相同。
- 再次增量扫描只读取 1 个活跃文件、跳过其余 1,539 个文件，期间新增 3 个请求；原有事实继续保持一致。SQLite `integrity_check` 返回 `ok`。

## 性能记录

以下为 Debug CLI 的 `/usr/bin/time -l` 结果；当时还有运行中的 App 与其他宿主进程，不是独占机器的性能上限。

| 操作 | 耗时 | 最大 RSS |
| --- | ---: | ---: |
| 完整历史重扫 | 184.721 秒 | 133.1 MiB |
| 历史重新计价 | 57.867 秒 | 26.0 MiB |
| 增量扫描 | 0.836 秒 | 32.8 MiB |

## 周额度修复

真实观测揭示：只凭 `resets_at` 改变生成历史候选，会把额度截止时间漂移误当成重置。修复前历史前 10,000 行中有 2,642 行属于零用量的待确认变化。

现仅在跨过原计划边界且重置时间推进，或已用百分比回落时生成候选。提前回落即使重置时间不变、已经产生新用量也能识别；账号不明确则保留待确认。仅截止时间漂移时保留原观测，不生成重置历史。

同一真实副本修复后返回完整 3,829 条候选（无下一页）：自然边界 3,072、账号明确的疑似提前重置 1、未知归属回落 756；零用量漂移候选为 0。这些包含不同任务各自的未知账号证据，不能称为 3,829 个独立账号周期，也不能累计百分比。

`LimitQueryTests` 补充零及非零用量的截止时间漂移、同一截止时间下百分比未降至零的提前重置；与 `UsageStoreTests` 合计 11 个测试通过。数据库新版本拒绝打开时，现在明确提示更新 TokenTick，不再只显示错误编号。

## 接口及分发

- 干净 Release 构建、ZIP 解压、SHA-256、App／CLI 严格签名验证通过，两个可执行文件均只含 arm64。使用 ad-hoc，无付费 Developer Program 和公证。
- 解压后的 CLI 通过真实接口读取 3 个额度窗口，包含周额度、五小时额度及额外额度桶。SQLite 只保存其中 2 个周额度观测，实时完整快照不进入持久同步报告。
- 独立空价格库实际请求 models.dev 成功，保存 48 个模型的首份快照；4 个图像模型仍明确缺少 token 价格。已有库同日再次同步直接返回已同步，不重复请求或新增价格。

## 本机证据

原始数据库与含账号的接口响应只留在忽略目录，不提交仓库：

- `.build/audit/current-history-qz20kqpt/`：迁移前／后数据库、`verification.json`、五维直接查询及缓存 JSON、计价和扫描报告、周额度修复前后结果、接口及价格响应。
- `.build/audit/current-history-verify.py`：基础扫描与重算核对；直接查询证明另见 `verification.json` 的 `direct_query_proof`，确认持有写锁且缓存版本缺失。
- `.build/logs/current-history-fixes-tests.log`、`current-history-cli-build.log`：本轮修复测试与 CLI 构建。
- `.build/releases/local-NtTiPp/`：基线提交的干净 arm64 Release ZIP 和校验文件。

## 当前设备 App 升级与界面

用户解锁后，`script/build_and_run.sh --verify` 成功启动当前 Debug App，日志为 `.build/logs/current-runtime-upgrade.log`。Computer Use 按完整 App 路径选择实例，避免误开旧解压包；初始化用的临时旧目录映射已移除。

默认数据库实际迁移至 `v5.weekly-limit-observations`，`pricing_algorithm=2`，没有待重算标记，`statistics_dirty=false`。一次升级后采样有 168,987 条请求、20,656,353,773 tokens、147,165 条历史周额度观测；后续活跃日志持续增加，不作为固定快照基线。后续本地同步发现 1,540 个文件、仅扫描 2 个、跳过 1,538 个，新增 2 条请求，没有采集或同步问题。原始检查记录在 `.build/audit/current-default-upgrade.json`。

原生界面已观察到：总览重算后显示 10.62B tokens、US$8,266.57 已知金额和 836.54M 未定价 tokens，提供精确整数辅助信息；额度页显示当前 3 个 API 窗口，包含 300／10080 分钟及额外桶，历史区域仅周额度，并明确标记疑似提前重置／未知归属证据。数值会随同步更新。

设置页的目录选项移除已由源码和测试验证；本次菜单操作遇到工具元素失效及按键名称不支持，未补记新版设置页交互通过。其他精细 UI、完整键盘和 VoiceOver 不作为当前完成门槛。

最新代码 `854f0ded9a31` 的干净 App／CLI Release 包位于 `.build/releases/local-wz3odX/`，构建、仅 arm64 和严格签名检查通过，日志 `.build/logs/current-package-final.log`。
