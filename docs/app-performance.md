# App 运行资源验证

环境：Apple M2 Pro、macOS 27、Xcode 27 beta。使用通用 Release App 的 arm64 进程，数据为既有冻结日志的独立 APFS 副本及隔离 SQLite；不扫描或写入产品默认数据库。测试输入包含 1,539 个 rollout 文件，其中 1,538 个压缩文件和一个普通 fixture，预期 168,166 条用量、20,534,374,503 tokens、816,059,149,600 纳美元。

## 文件通知触发自身扫描

2026-09-10 的 `6a66f3f` 基线运行发现：首次同步后文件未改变，但 App 约每 10.5 秒再次执行本地扫描，每次报告读取 0 字节。独立 FSEvents 观测在同一时刻仅记录 `state_5.sqlite-shm` 的变化（`0x00011400`）。

`ThreadCatalogReader` 已使用 SQLite 只读连接，但 WAL 读者仍可更新共享内存中的同步状态；原监听器接受所有 `state_` 前缀事件，因而将自己的读取误当成新数据，扫描结束后继续触发扫描。修复只排除 `state_*.sqlite-shm` 事件，保留数据库主文件、WAL、日志、任务目录映射以及丢失事件后的完整核对。没有启用会忽略活跃 WAL 的 immutable 读库方式，也没有延长正常扫描间隔来隐藏问题。

原生文件事件回归测试验证：更新现有 SHM 文件不会通知扫描，随后更新 WAL 和主文件仍能通知；原有新增目录、日志追加和归档测试保持通过。`AutomaticSyncTests` 共 6 个测试通过，日志 `.build/logs/watcher-shm-tests.log`；修改后的通用 Release App 构建通过，日志 `.build/logs/watcher-shm-release-build.log`。

## 运行采样

- 驱动脚本：`.build/audit/app-resource-profile.py`。按 0.5 秒采样 App PID 的 RSS、`ps` CPU 百分比及累计 CPU 时间，并保存每次完成的同步报告；不将 profiler 或 Codex app-server 子进程用量算入 App。
- 修改前基线：`.build/audit/app-resource-a3hqhkul/`。首轮同步报告耗时约 103.739 秒，采样在约 105.644 秒观察到完成，扫描约 9.165 GB 解压后数据，本地问题数为 0。首轮随后完成十分钟采样，共 1,372 个样本、59 份观测报告；稳态平均 CPU 37.43%（单核 100%），采样 RSS 峰值 244.44 MiB。`verification.json` 确认 168,166 条用量的全部字段与参考库双向 SQL 差异均为 0，完整性为 `ok`。
- `file-events.log` 保存导致反复扫描的原始文件事件，`samples.csv` 保存曲线数据，`reports.json` 保存完成报告。Mac 在采样期间锁定，后续稳态属于后台观测，具体边界记录在 `ui-observations.json`。
- 冻结 Codex 目录未包含认证文件，API 阶段报告 `-32600`；该隔离限制不能冒充真实账号 API 性能。价格沿用既有历史快照，自动同步仍按产品逻辑执行。
- 修改后已用相同输入重跑，恢复定时补扫且全部用量字段不变。采样峰值不是未采样瞬间的绝对峰值，短期后台曲线也不代表长时间前台交互或 macOS 26／Intel 验收。

本机固定输入的持续运行已完成；不据此宣布所有前台交互和目标系统资源验收完成。

### 修改后采样的观测修正

首个修复后任务 `.build/audit/app-resource-c0get1di/` 因驱动断言失败中断，未计为完成或 App 崩溃：App 的“最近同步报告”已被首次同步后立即完成的补扫覆盖，旧驱动错误要求第一份被轮询观察到的报告必须扫描全部文件。该任务的原始 CSV、报告与 `interruption.json` 保留。

修正后任务 `.build/audit/app-resource-tlg770wi/` 仍从相同冻结输入和空库开始，采样三十分钟稳态。驱动允许首个观测报告已是补扫，首次完成时间明确记为观测上界；最终仍要求全部用量字段与参考库一致，不通过放宽数据核对来绕过失败。任务已完成，前后条件差异见下文，不宣称严格同条件的改善比例。

### 真实系统睡眠与恢复

运行期间主机于 2026-09-10 09:00:37 +0800 因合盖进入睡眠，随后有多次 DarkWake／维护睡眠，09:35:33 因开盖／HID 活动唤醒。原始电源记录保存在修复后目录的 `power-events.log`，本任务没有主动改变系统睡眠或电源设置。

同一 App 进程在 09:35:33.344 开始恢复全来源同步，约 1.575 秒结束；随后一次事件补扫，再恢复约 61 秒的本地定时核对，没有逐次重放睡眠期间错过的计时器。唤醒后的每日页面 AX 精确值为 10,497,014,164 tokens，9 月 10 日为 47,865,919，与隔离 CLI 同范围查询一致（`after-wake-ui.json`、`after-wake-day.json`）。返回截图为缩略图，不据此宣布完整可读性验收。

采样器的 monotonic 时钟不包含完整深睡时间，同步报告使用实际 UTC 时间；图中资源曲线与同步事件分别标明时轴。修复前后电源／锁屏状态不同，不能把两次均值之差直接解释为严格同条件的提升比例。最终持续运行与完整性核对通过，数据见下表。

### 原生窗口观测边界

09:52 左右经 Finder 的“前往文件夹”选择已有 Release App 并打开后，Computer Use 返回完整 1120×761 深色任务窗口，9 月 9 日筛选总量为 387,015,234 tokens，数值列完整可读；同日隔离 CLI 查询精确一致，证据为 `front-ui-day-cli.json`。之前的缩略图不能追溯算作通过；此次前台检查发生在资源采样末段，条件保存在 `ui-front-observation.json`。菜单栏系统进程仍返回工具超时，完整菜单交互和 VoiceOver 不据此宣布通过。

## 修复后的完成结果

干净源码 `6bcaae14da6f` 的 Release App 在首次观测同步完成后持续采样 1,800 秒，共 3,703 个样本、32 份观测同步报告；进程未崩溃，驱动退出码为 0，核对后由驱动退出其自行启动的 App。首轮报告耗时 100.964 秒、观测完成上界 101.657 秒，读取 9,165,391,986 解压后字节。后续报告全部读取零字节、本地问题数均为 0。

最终 168,166 条用量、20,534,374,503 tokens、816,059,149,600 纳美元与参考库一致；全部用量字段（排除自增 ID）双向 SQL 差异均为 0，数据库完整性为 `ok`。该金额是已知部分，不能视为缺价历史的完整费用。

| 观测范围 | 平均 CPU（单核 100%） | RSS 采样峰值 | RSS 采样中位数 | 结束 RSS |
| --- | ---: | ---: | ---: | ---: |
| 修复前首次同步后 10 分钟 | 37.43% | 244.44 MiB | 164.77 MiB | 149.95 MiB |
| 修复后首次同步后 30 分钟 | 2.54% | 242.09 MiB | 179.11 MiB | 170.77 MiB |

修复后前 600 秒均值为 1.25%，但包含不同的睡眠条件，仍不能作为严格同条件对照。唤醒后的普通本地补扫间隔约 60–63 秒；五分钟全来源同步后仍可能有一次约 10 秒的文件事件补扫，没有修复前持续的自触发循环。修复主要改善不必要的 CPU 工作；本次数据不支持宣称 RSS 中位数下降。

另一次系统 `footprint` 快照记录当前物理占用 108,414,224 bytes、进程历史峰值 169,018,688 bytes（`footprint.json`）；该指标与 RSS 不同，不混算成曲线或绝对内存峰值。

原始结果为修复后目录的 `verification.json`、`comparison.json`、`samples.csv`、`reports.json`；`resource-comparison.png`、`.svg`、`.pdf` 已生成并核对可读性。资源轴使用单调时钟，事件轴使用实际时间，分别显示睡眠间隔。绘图脚本为 `.build/audit/plot-app-profile.py`。这些文件和冻结日志副本均留在忽略目录，不进入分发包。

验证结束后通过 `./script/build_and_run.sh --verify` 构建并恢复正常 Debug App，构建与启动通过。原生设置确认 Codex 目录为 `/Volumes/Projects/.codex`、数据库为用户 Application Support 下的 TokenTick 库、自动同步为开启，隔离性能测试未替换这些持久设置。
